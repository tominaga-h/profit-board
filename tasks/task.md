# タスク一覧：ProfitBoard

> プラン本体: [plan.md](./plan.md)（決定事項A1〜A8・アーキテクチャ決定・リスクはそちらを参照）
> 仕様変更v1のプラン: [SPEC-MODIFY-1-PLAN.md](./SPEC-MODIFY-1-PLAN.md)（`docs/SPEC-MODIFY-1.md` の3変更に対応するタスク設計）
> 原則: `docs/SPEC.md` を正とする。部門・PMカラムは存在しない（A1/A2で削除確定）。

---

## フェーズ1: 基盤

### Task 1: Nuxt 3 スキャフォールドと共通レイアウト

**内容:** Nuxt 3 (TypeScript, `ssr: false`) を初期化し、`@nuxtjs/tailwindcss`・`@nuxtjs/supabase` を導入。デザイン共通のサイドバー（ロゴ、ナビ、ユーザー表示）＋メインコンテンツのレイアウトと、全ページの空ルーティングを作る。レポート/設定は非活性（A7）。

**受け入れ基準:**

- [x] `npm run dev` で `localhost:3000` が起動し、サイドバー付きレイアウトが表示される
- [x] `/dashboard` `/projects` `/projects/edit` `/performance/input` `/members` `/members/edit` `/login` の空ページが存在しナビから遷移できる
- [x] `.env.example` に `SUPABASE_URL` / `SUPABASE_KEY` を記載

**検証:** `npm run build` 成功。手動でナビ遷移確認。
**依存:** なし
**触るファイル:** `nuxt.config.ts`, `app.vue`, `layouts/default.vue`, `components/AppSidebar.vue`, `pages/**/*.vue`, `.env.example`
**規模:** M

### Task 2: DBスキーマ・RLS・シードデータ

**内容:** SPEC 5.1 の5テーブルのDDL（**カラム追加なし・SPEC通り**）、`is_app_user()` 関数（`SECURITY DEFINER`）と全テーブルのRLSポリシー（SELECT/INSERT/UPDATE/DELETE ※削除制御はアプリ側で行うがマスタ行削除にDELETEが必要）、`updated_at` 自動更新トリガ、初期管理者＋動作確認用シードSQLを作成しSupabaseに適用する。

**受け入れ基準:**

- [x] マイグレーションSQLがSupabaseでエラーなく実行でき、5テーブル＋関数＋ポリシーが作成される
- [x] 未認証（Anon Keyのみ）のリクエストでは全テーブルが0件になる（RLS遮断）
- [x] `m_users` に登録済みメールの認証ユーザーでは各テーブルへCRUDできる（※下記注記）

> **基準3の検証範囲:** 実際のGoogle OAuthのJWTはTask 3で初めて得られるため、ここでは
> `request.jwt.claims` とロールを偽装してポリシーを実際に評価させる方法で検証した。
> 登録済みメールでCRUD全操作が成功し、`m_users` 参照時に無限再帰（42P17）が起きないこと、
> 未登録メールでは0件かつINSERTが42501で拒否されることを確認済み。
> **実JWTでの最終確認はTask 3で完了した。** 登録済みアカウントでのログイン後、
> サイドバーに `m_users` の実データ（姓名）が表示された。これは実際のGoogle OAuthの
> JWTのメールクレーム経由でRLSを通過して読めたことを意味し、偽装JWTでの検証結果が
> 本番経路でも成立することの裏付けになる。

**検証:** Supabase SQL Editor / RESTで認証あり・なしのアクセス結果を確認。
**依存:** なし（適用確認はT3後でも可）
**触るファイル:** `supabase/migrations/20260809120000_init_schema.sql`, `supabase/migrations/20260809120100_rls_policies.sql`, `supabase/seed.sql`, `docs/SETUP.md`（新規・初期管理者手順）
**規模:** M

> **マイグレーションのファイル名について:** Supabase CLI は `<14桁タイムスタンプ>_<名前>.sql`
> をマイグレーションの `version` として扱うため、当初想定していた `0001_schema.sql` の
> 連番形式は使えない。上記の実ファイル名がその形式に従っている。
>
> **シードの範囲について:** ユーザーの判断により初期管理者1名のみとし、動作確認用の
> ダミー実績データは作らない。Task 11（ステータス判定4パターン）・Task 12（前年同期比）の
> 検証データは、Task 8〜10 の入力画面から作成するか、その時点で別途用意する。

### Task 3: Google OAuth 認証とルートガード

**内容:** ログインページ（「Googleでログイン」）を実装。Supabase AuthのGoogle OAuth完了後、メールを `m_users.email` と照合し、不一致なら即サインアウト＋ログイン不可メッセージ表示（SPEC 3.1）。認証必須ミドルウェアで未認証を `/login` へ、認証済みの `/login` アクセスを `/dashboard` へリダイレクト。サイドバー下部にログインユーザー名を表示し、サインアウト導線を付ける。

**受け入れ基準:**

- [x] `m_users` 登録済みGoogleアカウントでログイン→ `/dashboard` へ遷移
- [x] 未登録アカウントはログイン不可メッセージが出て、セッションが残らない ※未確認
- [x] 未認証で `/dashboard` 等にアクセスすると `/login` へリダイレクト

> **基準1は実ログインで確認済み。** `tominaga_h@mad2007.co.jp` でログインし、
> `/dashboard` への遷移とサイドバーへの「冨永 隼人」＋メール表示を確認した。
>
> **基準2（未登録アカウントの拒否）は未確認のまま残している。**
> 正常系とは別の経路のため、基準1が通っても検証したことにはならない。
> ここが動かないと任意のGoogleアカウントで業績データにアクセスできてしまうため、
> 未登録のアカウントでログインを試し、以下を確認すること。
>
> 1. `/login` に戻される
> 2. 「このGoogleアカウントは利用登録がありません」が表示される
> 3. サイドバーのある画面に入れない
>
> **ログインなしで確認済みの事項:**
>
> - 基準3: `/dashboard`・`/members` へ直アクセス → `/login` へリダイレクト（ブラウザで確認）
> - Google プロバイダが有効（`/auth/v1/settings` が `"google":true` を返す）
> - 認可エンドポイントが `accounts.google.com` へリダイレクトし、`redirect_uri` が
>   Supabase のコールバック、`redirect_to` が `/confirm`、`scope` に `email` を含むこと
>   （手順は `docs/SETUP.md` 7.4）
> - ビルド成功・`vue-tsc` の型チェック成功

**検証:** 登録済み/未登録アカウント両方で手動ログイン確認。
**依存:** T1, T2
**触るファイル:** `pages/login.vue`, `pages/confirm.vue`（新規）, `middleware/auth.global.ts`, `composables/useAppUser.ts`, `components/AppSidebar.vue`, `types/database.types.ts`（新規・自動生成）, `nuxt.config.ts`, `docs/SETUP.md`
**規模:** M

> **設計上の判断:** `@nuxtjs/supabase` の内蔵リダイレクトは無効化した（`redirect: false`）。
> 内蔵ガードはセッションの有無しか見ず、`m_users` 未登録のGoogleアカウントを通してしまうため。
> 判定は `middleware/auth.global.ts` に一本化している。
>
> **Task 2 の受け入れ基準3について:** 実JWTでの最終確認はTask 3で行うとしていたが、
> これも上記の実ログイン時に併せて確認する（登録済みアカウントで各画面のデータが
> 読み書きできれば、RLSが実JWTのメールクレームで機能していることの確認になる）。
>
> **ついでに修正した既存の不具合:**
>
> - `nuxt.config.ts` の `colorMode.preferred` は誤り（正しくは `preference`）。
>   誤ったキーは無視されるため、OSがダークモードだと画面が暗転する状態だった。
> - `.env.example` に `SUPABASE_SERVICE_ROLE_KEY` / `DB_PASSWORD` が不足していた。

### ✅ チェックポイント1（基盤）

- [x] ビルド成功・認証フローが端から端まで動く・RLSが未認証を遮断する
- [x] ここで人間レビュー

---

## フェーズ2: 共通ロジック

### Task 4: 計算・判定ロジック composable と単体テスト

**内容:** Vitestを導入し、純粋関数として実装：①稼働人日 `hours/8`（小数2位、A5）②人件費 `work_days × unit_price` ③売上/費用合計・粗利・粗利率（売上0なら0%、SPEC 6.1）④ステータス判定（成長/順調/注意/警告＋前月データなし時の規則、SPEC 6.2）⑤年度ユーティリティ（7月始まり、年度⇔暦年月変換、今年度算出）⑥前年同期比（A6の定義、過去データなしは `null`→「-」表示）。

**受け入れ基準:**

- [x] SPEC 6.1/6.2 の全計算・全判定分岐（境界値: 利益0、前月比0、前月なし、売上0）にテストがある
- [x] `npx vitest run` が全件パス

**検証:** `npx vitest run` / `npm run build` 成功。
**依存:** T1（T2/T3と並行可）
**触るファイル:** `lib/calc.ts`, `lib/statusJudge.ts`, `lib/fiscalYear.ts`, `tests/unit/*.spec.ts`, `vitest.config.ts`, `Makefile`, `nuxt.config.ts`, `.gitignore`, `package.json`
**規模:** M

> **検証結果:** テスト89件が全件パス。カバレッジは本タスクで追加した3ファイル
> （`lib/calc` / `lib/fiscalYear` / `lib/statusJudge`）がいずれも
> **Stmts・Branch・Funcs・Lines すべて100%**。`npm run build` も成功。
> テストは Docker 経由で実行する（`make test` / `make test-cov`）。
> ホストで `npx vitest run` するとコンテナ（node:24）とNodeバージョンが異なる。
>
> **確定した仕様判断（SPECの曖昧さをユーザー確認で解消）:**
>
> | 論点 | 決定 |
> | --- | --- |
> | 稼働人日の丸め | 小数第2位で**四捨五入**（SPEC 4.3「切り捨てまたは適切な桁調整」が曖昧だったため） |
> | 人件費の元値 | **丸めた後の人日** × 単価（画面の人日×単価＝表示金額となり検算が合う） |
> | 前年同期比の月範囲 | 当年度で**実績のある月だけを集合**で比較。飛び月があっても両年度で同じ月だけを足す |
>
> **★ 丸め実装の落とし穴（最重要）:** `toFixed` も素朴な `Math.round(v*100)/100` も
> 四捨五入として**誤り**。実入力範囲（`work_hours` は `NUMERIC(6,1)`）で全数検証した結果、
>
> - `toFixed(2)`: 0.0〜200.0h の範囲だけで **169件**が期待と食い違う
>   （例: 0.6h → 0.075 を `0.07` にする。正しくは `0.08`）
> - `Math.round(v*100)/100`: `1.005` を `1.00` にする（乗算時に 100.49999… となるため）
>
> 採用した**指数表記方式**（`Number(\`${v}e2\`)` 経由）は10進の桁移動になり両方の罠を回避する。
> 実入力ドメイン全域（0.0〜999.9h）で数学的に正しい四捨五入と一致することを確認済み。
> `roundTo` の 0.615 / 1.005 のテストは、実装を上記どちらかに書き換えたら落ちる回帰テスト。
>
> **SPECの空白を埋めた判断:**
>
> - **売上0・費用あり** → 粗利率 **0%**（SPEC 6.1-⑥ 明記）。粗利は赤字なのに率が0%という
>   一見ちぐはぐな表示になるが仕様通り。実装にコメントで意図を残した
> - **前年同期比で前年度が0以下** → `null`（「-」表示）。A6に定義がなく増減率が定義できないため
> - **前年同期比で前年度側に穴がある月** → 0として足す。月の集合は当年度側で確定させる
>   （積集合を取ると前年度の穴だけで当年度の実績が比較から消えてしまう）
>
> **設計上の判断:**
>
> - **`@nuxt/test-utils` は使わない。** 対象が Nuxt ランタイムに触れない純粋関数のみで
>   スタブする対象がなく、Nuxt インスタンスのビルドで起動が数十秒増えるだけのため。
>   実測でテスト全件が1秒未満で完了している。コンポーネントテストが必要になった時点で追加する
> - **`composables/` ではなく `lib/` に置き、`use` プレフィックスを外した**（レビュー指摘）。
>   これらは Vue のリアクティビティに依存しない純粋関数の集まりでライブラリに近く、
>   composable の慣習（`useXxx()` を呼んで ref を受け取る）に当てはまらないため。
>   `composables/` に残るのは Supabase クライアントと `useState` に依存する
>   `useAppUser.ts` だけ。`lib/` は auto-import 対象外なので呼び出し側で明示的に import する
> - **年度を7月始まり（6月決算）に変更した**（レビュー指摘）。当初は4月始まりで実装したが、
>   自社が6月決算のため 7月〜翌6月を1年度とする。「2026年度」は 2026年7月〜2027年6月。
>   開始月は `lib/fiscalYear.ts` の `FISCAL_START_MONTH` 1ヶ所だけで定義し、月の並び
>   （`FISCAL_MONTHS`）・年度⇔暦年変換・前年同期比はすべてこれを参照するので、
>   将来また変わってもこの1行を直せばよい。SPEC 6.0 に年度の定義を明記した
> - **`getCurrentFiscalYear(baseDate = new Date())` と基準日を注入可能にした。**
>   関数内で `new Date()` を直接呼ぶと「7月1日に実行したら落ちる」時限爆弾テストになる
> - **`vitest.config.ts` で `~/` エイリアスを直書きした。** `.nuxt/tsconfig.json` は生成物で
>   gitignore 済みのため、クリーンチェックアウトでは `vite-tsconfig-paths` が解決できない
>
> **`typeCheck: true` を有効化した（`nuxt.config.ts` のコメント通り）。**
> これにより `npm run build` に `vue-tsc` が載る。有効化時に `vitest.config.ts` の
> `node:url` インポートが `TS2307` で落ちたので、**`@types/node` を devDependencies に
> 追加**して解決した（`vitest.config.ts` はNode上で動く設定ファイルであり、Node の型が
> 必要なのは当然。`tsconfig.json` への `types` 追記は不要で、TypeScript が `@types/*` を
> 自動で拾う）。バージョンはコンテナの Node 24 系に合わせて `24.13.3` で固定。

---

## フェーズ3: マスタ管理

### Task 5: メンバー一覧画面（`/members`）

**内容:** `m_users` 全件をテーブル表示（ID・姓・名・メールアドレス・単価。SPEC 4.4 の項目のみ、A8により検索・フィルタ・CSV・集計表示はスコープ外）。右上の「編集する」ボタンで `/members/edit` へ遷移。

**受け入れ基準:**

- [x] シードデータのメンバーがID・姓・名・メール・単価の列で一覧表示される ※未確認
- [x] 「編集する」で `/members/edit` へ遷移できる ※未確認

**検証:** dev環境で表示を手動確認。
**依存:** T3
**触るファイル:** `pages/members/index.vue`, `composables/useMembers.ts`, `lib/format.ts`, `lib/fetchStatus.ts`, `tests/unit/format.spec.ts`, `tests/unit/fetchStatus.spec.ts`
**規模:** S

> **検証結果:** テスト109件が全件パス（Task 4 の101件 + 本タスクの8件）。
> `lib/` 全5ファイルのカバレッジは **Stmts・Branch・Funcs・Lines すべて100%** を維持。
> `make build`（`typeCheck: true` により `vue-tsc` が走る）も成功。
>
> **★ 両受け入れ基準はブラウザでの目視確認が未実施。** ユーザーの判断により本タスクの
> 検証はビルド確認までとした。画面を開いての確認は別途行うこと。シードデータは
> メンバー1名（冨永 隼人 / ¥60,000）のみなので、一覧に1行出れば基準1を満たす。
> **シード未適用（`make db-push-seed` 未実行）だと0件表示になる**ので、
> 確認前にシードが入っているかを見ること。
>
> **確定した仕様判断:**
>
> | 論点 | 決定 |
> | --- | --- |
> | ID の表示書式 | **裸の整数**（1, 2, 3…）。デザイン画像の `001` ゼロ埋め3桁は採用しない |
> | ローディング/エラー/0件 | **本タスクで実装**（当初 T15 の予定を前倒し） |
>
> ID をゼロ埋めにしなかったのは、plan.md 冒頭の原則「デザイン画像は参考であり、
> 生成時に付加された要素は採用しない」に従ったため。SPEC 4.4 にも 5.1 のDDLにも
> ゼロ埋めの根拠がなく、`m_users.id` は `SERIAL`。1000件超で桁が破綻し、
> Task 6 の編集画面（ID は編集不可の表示のみ）とも書式を揃える必要が出る。
>
> **A8 により実装しなかったもの**（デザイン画像 `member-list.png` にはある）:
> 検索ボックス・「所属」プルダウン（A1で部門は存在しない）・「単価帯」プルダウン・
> CSVエクスポート・平均単価/単価合計の集計・ページネーション・パンくず「設定 › メンバー」。
> デザインから採用したのは5列構成と配色（ID淡色・姓名太字・メールグレー・単価右寄せ）のみ。
>
> **設計上の判断:**
>
> - **`UTable`（@nuxt/ui v2）は使わず素の `<table>` + Tailwind にした。** `UTable` の
>   `rows` は `TableRow[]` で `TableRow` が `{ [key: string]: any }` のため、
>   列スロットで受け取る行が `any` に潰れる。`typeCheck: true` を有効にしている意味が
>   薄れ、`member.unti_price` のようなタイポがビルドを素通りする。加えて後続タスクにも
>   波及しない（T6/T7 の `EditableTable` は全セルが `<input>`、T14 は「1プロジェクト=4行」
>   の `<tbody>` グループで `UTable` の「1行=1 `<tr>`」では構造的に表現できない）
> - **`lib/format.ts` を新設した。** 金額表示はこの画面が初出だが、T9（実績入力の
>   リアルタイム集計）・T11（KPI・売上・費用・利益）・T12（KPIカード）・T14（マトリクス）
>   すべてで必要になる。ページ内 computed にすると4画面に重複する。
>   `Intl.NumberFormat` の `style: 'currency'` は**使わない**（環境の ICU によって
>   全角 `￥`(U+FFE5) や `JP¥` になる）。区切りだけ Intl に任せ半角 `¥` を自前で前置する。
>   負値は `-¥1,000` と符号を先頭に置く（Intl 任せだと `¥-1,000` になり、
>   T11 の赤字一覧でマイナスが記号に埋もれる）
> - **`lib/fetchStatus.ts` を composable の外に出した。** T7 は `useProjects.ts` という
>   別ファイルを作る計画なので、`useMembers.ts` 内に閉じ込めると同じ列挙を2回書くことになる。
>   `pending: boolean` + `error` の2変数にしないのは、「まだ取得していない」と
>   「取得したが0件」がどちらも `pending=false・error=null・rows=[]` になり区別できず、
>   初回描画で一瞬「0件です」が出るため（`AppUserStatus` と同じ理由）。
>   **「0件」はステータスに入れない**——0件は失敗ではなく成功の一形態で、件数は
>   `rows.length` で判る。混ぜると T6 の「0件だが保存はできる」状態が表現できなくなる
> - **`useMembers` は `useState` ではなくローカル `ref` を使う。** `useAppUser` が
>   `useState` なのは全ページ常駐のサイドバーが同じ状態を見るためで、一覧画面には
>   当てはまらない。むしろ T6 で保存して一覧に戻ったとき古いキャッシュが残るほうが害になる
> - **`useAsyncData` / `useFetch` は使わない。** `useFetch` は HTTP エンドポイント向けで
>   Supabase クライアントには使えない。`useAsyncData` は `ssr: false` では実質
>   「マウント後に非同期関数を呼ぶ」以上のことをせず、`error` が `NuxtError` に
>   ラップされて `PostgrestError` の情報（code/details/hint）が失われる
> - **取得は `onMounted`。** トップレベル `await` にすると Suspense でちらつきは消えるが
>   取得完了までページ全体が真っ白になる。既存の `confirm.vue` も `onMounted` を使っている。
>   `IDLE` をローディング側の枝に含めているのは、`onMounted` が走る前の1フレームで
>   `v-else` に落ちて「0件です」が一瞬見えるのを防ぐため
> - **`order('id')` は省略しない。** PostgREST は `ORDER BY` がないと行順を保証せず、
>   T6 で UPDATE した行だけが末尾に飛ぶ挙動になりうる
>
> **★ `lib/fetchStatus.ts` にもテストを書いた。** 定数だけなので不要と見込んでいたが、
> `as const` オブジェクトリテラルは実行される文なので、どのテストからも import されないと
> カバレッジ0%になり全体が98.64%に落ちた。`lib/**/*.ts` がカバレッジ対象である以上、
> `lib/` にファイルを足すときは定数だけでもテストが要る。

### Task 6: メンバー編集画面（`/members/edit`）

**内容:** テーブル形式のインライン一括編集（姓・名・メール・単価、IDは自動採番・編集不可）。「追加」で末尾に新規行（IDは保存時に自動採番）。ゴミ箱で行削除するが、`t_costs` に実績が1件でも存在するメンバーはエラーアラートを出して削除拒否（SPEC 4.4）。「保存する」で新規INSERT・変更UPDATE・削除DELETEを一括実行。

**受け入れ基準:**

- [x] 既存行の編集・新規行の追加・実績のない行の削除が保存で反映される ※未確認
- [x] `t_costs` に実績のあるメンバーの削除はアラートが出て拒否される ※未確認
- [x] キャンセルで変更を破棄して一覧へ戻る ※未確認

**検証:** 実績あり/なしメンバー両方で削除操作を手動確認。`npx vitest run` パス継続。
**依存:** T5
**触るファイル:** `pages/members/edit.vue`, `composables/useMembers.ts`, `lib/schemas/member.ts`, `tests/unit/schemas/member.spec.ts`, `package.json`
**規模:** M

> **検証結果:** テスト127件が全件パス（T5までの109件 + 本タスクの18件）。
> `lib/` 全6ファイルのカバレッジは **Stmts・Branch・Funcs・Lines すべて100%** を維持。
> `make build`（`typeCheck: true`）も成功。
>
> **★ 受け入れ基準3件はいずれもブラウザでの目視確認が未実施。**
> とくに**基準2は現状のデータでは検証できない**。`t_costs` にシードがなく実績が
> 1件も存在しないため、「実績のあるメンバー」を作れない。確認するには
> `t_costs` に手動で1行入れるか、Task 8〜10（実績入力）の完成を待つ必要がある。
>
> **確定した仕様判断:**
>
> | 論点 | 決定 |
> | --- | --- |
> | 自分自身の削除 | **禁止**（SPEC外だが実装）。ゴミ箱を `disabled` にし理由を `title` で示す |
> | 削除判定のタイミング | **ゴミ箱押下時に即判定**（SPEC 4.4 の文言に忠実） |
> | 保存の原子性 | 失敗時は**再取得して実状態を見せる**（部分適用を隠さない） |
> | 入力検証 | **Zod で行単位に検証**（メール形式含む） |
>
> **★ 自分自身の削除を禁止した理由（SPECに規定はないが実装した）:**
> RLS の `m_users_delete_app_user` は `is_app_user()` しか見ておらず、行の絞り込みが
> ない。つまり**自分の行も消せる**。消した瞬間 `is_app_user()` が false になり、
> 全テーブルへのアクセスが遮断されて再ログインもできない。復旧手段は Supabase の
> SQL Editor から手動 INSERT するのみ。しかも初期管理者は実績を持たないので
> SPEC 4.4 の削除ガード（実績があれば拒否）を素通りする。シードが管理者1名である
> 以上、事故れば**全員がログイン不能**になる。
> なお**メールアドレスの変更も同種の危険がある**（変更後の JWT 照合に失敗する）が、
> 削除と違って「意図的な変更」の可能性があり、SPEC 外の推測が増えるためガードしない。
>
> **Zod を導入した（`zod@3.25.76` を devDependencies に固定）:**
>
> - **★ Zod 4.4.3 を入れようとして失敗した。** `@nuxt/ui` 2.22.3 の
>   peerDependencies が `zod@^3.24.0`（optional）で、npm が ERESOLVE で拒否する。
>   `peerOptional` は「省略してよい」であって「どのバージョンでもよい」ではない。
>   `--legacy-peer-deps` で押し通せるが、依存解決全体の整合性チェックを
>   Zod のためだけに無効化するのは割に合わないため 3 系の最新にした。
>   なお `UForm` の実装は `"~standard" in schema` で分岐しており、
>   Standard Schema 準拠なら 4 系でも動く（3.25 系も `~standard` を持つ）
> - **`UForm` は使わない。** 「1フォーム = 1オブジェクト」のモデルで、
>   N行×M列のテーブルとは噛み合わない（行数ぶん `UForm` がネストする）。
>   Task 5 で `UTable` を退けたのと同じ、部品の想定と用途が合わないケース
> - **`.trim()` は検証と同時に値を変換する**ので、保存値の正規化を兼ねている。
>   メールの前後に空白が残ると JWT のクレームと一致せず、
>   「登録できたのにログインできないメンバー」ができる
> - **`.toLowerCase()` はしない。** DB は `VARCHAR` で大小を区別するが、
>   SETUP.md が「Google アカウントのものと完全に一致させること」と指示している以上、
>   入力値を勝手に変換するほうが危険
> - **`invalid_type_error` は空欄対策。** `v-model.number` の空欄は `NaN` になり、
>   これがないと "Expected number, received nan" という英語が画面に出る
> - デザインの注意書きにある「メールアドレスは社内ドメインのみ登録可」は
>   **SPEC にも DDL にも根拠がない**ため文言から落とした（plan.md A1 と同じ判断）
>
> **設計上の判断:**
>
> - **`components/EditableTable.vue` は作らなかった**（task.md の当初の「触るファイル」から外した）。
>   Task 7 と共通なのは骨格だけで、列定義（メンバーは単価の数値入力あり／PJは文字列2列）も
>   削除判定（`t_costs` のみ／`t_sales`+`t_costs`）も完全に非重複。型の共通項は
>   `{ id: number }` しかない。1例だけで抽象化すると確実に外すので、
>   **Task 7 で2例目を見てから共通部分を抽出する**。なお task.md の Task 7 は
>   「触るファイル」に EditableTable を含まず、依存欄に「T6完了後ならそれを再利用」と
>   書かれているため、切り出すなら T7 の要件を先読みで吸収する必要があった
> - **保存の実行順は DELETE → UPDATE → INSERT。** 消したメンバーのメールを別の行に
>   付け替えるケースがあり、順序を変えると一時的に同じメールが2行存在して
>   UNIQUE 制約（`m_users_email_key`）に当たる
> - **変更のあった行だけ UPDATE する。** 全行を投げると無変更行の `updated_at` まで
>   動き、「誰がいつ触ったか」が追えなくなる。差分検出は「変更 N 件」バッジと
>   保存ボタンの活性制御にも使っている
> - **`id` は INSERT で送らない**（SERIAL のシーケンスがズレて次の INSERT が主キー衝突する。
>   `seed.sql` にも同じ注意書きがある）。**`updated_at` も送らない**（トリガが自動更新）
> - **`hasCostRecords` は失敗時に `true`（実績あり）へ倒す。** 判定できないまま削除を
>   通すと DB の FK 違反にぶつかって保存全体が途中で止まる。安全側は「消させない」
> - **FK 違反（23503）を握りつぶさない。** ゴミ箱押下時のチェックと実際の DELETE の間に
>   他の利用者が実績を登録する競合がありうるので、専用の文言で伝える
> - **`:key` に配列添字を使わない。** 行を削除すると後続の key がずれ、
>   入力中の値が別の行に移って見える。新規行は `crypto.randomUUID()` で採番する
> - **保存の進行状態に `FetchStatus` を使わない。** あれは取得専用で意味が合わない。
>   保存は「未実行」と「成功」を区別する必要がない（成功したら一覧へ遷移する）ので
>   `isSaving: ref<boolean>` で足りる
> - **画面内のメール重複は Zod では検知できない**（行単体のスキーマなので他行を知らない）。
>   保存前に配列全体で突き合わせ、**重複した両方の行**にエラーを出す
>   （片方だけだとどちらを直せばよいか分からない）
> - **取得に失敗しているときは編集させない**（テーブルを描画しない）。
>   空の状態から保存すると実データを消しかねない
> - **`v-model` はこのタスクがプロジェクト初出。** 子コンポーネントを作らないので
>   `defineModel` は不要で、`drafts` の要素に直接バインドしている

### Task 7: プロジェクト編集画面（`/projects/edit`）

**内容:** T6と同じインライン一括編集パターンで `m_projects` を編集（**サービス名・会社名のみ**。IDは自動採番・編集不可。A1/A2により部門・PM列はなし）。「追加」で末尾に新規行。`t_sales` または `t_costs` に実績が存在するプロジェクトの削除はエラーアラートで拒否（SPEC 4.5）。

**受け入れ基準:**

- [x] 追加・編集・実績なし行の削除が保存で反映される ※未確認
- [x] 実績のあるプロジェクトの削除はアラートが出て拒否される ※未確認
- [x] キャンセルで変更を破棄して戻る ※未確認

**検証:** 手動確認（実績あり/なし両方）。
**依存:** T5（`EditableTable` はT6完了後ならそれを再利用。T6と並行する場合は部品を先に切り出す）
**触るファイル:** `pages/projects/edit.vue`, `composables/useProjects.ts`, `lib/schemas/project.ts`, `lib/schemas/rowErrors.ts`, `lib/schemas/member.ts`, `tests/unit/schemas/project.spec.ts`
**規模:** M

> **検証結果:** テスト136件が全件パス（T6までの127件 + 本タスクの9件）。
> `lib/` 全8ファイルのカバレッジは **Stmts・Branch・Funcs・Lines すべて100%** を維持。
> `make build`（`typeCheck: true`）も成功。
>
> **★ 受け入れ基準3件はいずれもブラウザでの目視確認が未実施。**
> ただし Task 6 と違い、**基準2も確認できる見込みがある**。`m_projects` は
> シードが0件なので画面から自分でプロジェクトを作れる。実績側だけは
> Task 8〜10 が未実装のため SQL Editor から手で入れる:
>
> ```sql
> INSERT INTO t_sales (fiscal_year, month, project_id, category_small, amount)
> VALUES (2026, 7, <作ったPJのid>, '保守', 100000);
> ```
>
> **確定した仕様判断:**
>
> | 論点 | 決定 |
> | --- | --- |
> | 同名プロジェクトの重複 | **サービス名＋会社名の組で警告**（DB に一意制約がないため画面側で判定） |
> | `t_status` の削除ガード | **事前チェックに含める**（SPEC の文言にはないが FK は存在する） |
> | 遷移先 | `/projects`（Task 11 まで中身は空だが、サイドバーの導線と一致する） |
>
> **★ Task 6 から意図的に変えた点（member.ts を写すときの罠）:**
>
> - **`VARCHAR(255)`**。メンバーの姓名は `VARCHAR(50)` なので、
>   写経すると51文字以上のサービス名が画面で弾かれて登録できなくなる
> - **`m_projects` に UNIQUE 制約が一切ない。** `23505` のハンドリングを
>   持ち込んでも永久に到達しない死んだ分岐になるので書いていない。
>   同時に、**保存順序（DELETE → UPDATE → INSERT）に必然性がない**。
>   Task 6 では「削除した行のメールを別の行に付け替えると一時的に重複して
>   UNIQUE に当たる」という理由があったが、ここにはない。2画面で揃えているのは
>   読む側の都合であり、制約回避の意味を読み取らないよう `useProjects.ts` の
>   コメントに明記した
> - **自己削除ガード（`canDelete`）は持たない。** プロジェクトに「自分」の
>   概念がなく、`useAppUser` を参照する理由がない
>
> **★ 削除ガードは3テーブルを見る（SPEC 4.5 は2つしか挙げていない）:**
> SPEC 4.5 の文言は「`t_sales` または `t_costs` が存在するプロジェクト」だが、
> **`t_status` も `project_id` の外部キーを持つ**（`init_schema.sql` の t_status DDL）。
> 備考だけ登録されたプロジェクトは、`t_sales`/`t_costs` が空でも DB が
> FK 違反で削除を拒む。2つしか見ないと「削除できます」と画面で見せてから
> 保存時に落ちることになり、SPEC の意図（ゴミ箱押下の時点で拒否）に反する。
> 3つを `Promise.all` で**並列**に問い合わせている（順に await すると
> 往復が3回積み上がり、ゴミ箱を押してから戻るまでの待ちがそのまま3倍になる）。
> `idx_t_sales_project_id` / `idx_t_costs_project_id` は Task 2 が
> 「プロジェクト削除ガード（Task 7）」のコメント付きで用意済み。`t_status` は
> `UNIQUE (fiscal_year, month, project_id)` の先頭列が `fiscal_year` なので
> `project_id` 単独では索引が効かないが、年度×月×PJで1行しかできず件数が
> 小さいため許容した。
>
> **★ 重複チェックの区切り文字。** `service_name` と `company_name` を
> 連結して同一性を見るとき、区切りに ` ` (U+0001) を使っている。
> ハイフンなどの通常文字だと「AB」+「C」と「A」+「BC」が同じ組と
> 誤判定される。入力欄から打ち込めない制御文字であることが要点。
>
> **`toRowErrors` を `lib/schemas/rowErrors.ts` に抽出した:**
> Task 6 の `toMemberRowErrors` は実装が完全にジェネリックで、
> 型引数が `MemberRowInput` に固定されていただけだった。2例目
> （プロジェクト）が出たので実体を移し、`toMemberRowErrors` /
> `toProjectRowErrors` はそれぞれ型を束ねる薄い別名として残した。
> 既存の `member.spec.ts` 18件はラッパ経由で緑のままで、これが
> 抽出のリグレッションテストになっている。
>
> **★ `components/EditableTable.vue` は今回も切り出さなかった。**
> Task 6 で「Task 7 で2例目を見てから判断する」と保留した宿題への回答。
> 2例並べた結果、共通なのは「ヘッダ + `v-for` の行 + 追加ボタン + ゴミ箱」
> という **HTML の骨格だけ**だった。列定義（メンバー: 文字列3 + 数値1 +
> `¥` プレフィックス / PJ: 文字列2）も、削除判定（`t_costs` の1本 /
> 3テーブル並列）も、自己削除ガードの有無も重ならない。抽象化すると
> 「列定義の配列 + スロット + 削除判定コールバック」を受け取る部品になるが、
> それは素の `<table>` を書くより読みにくい。実際に重複しているのは
> `inputClass`（10行）程度。**Task 8〜10 の実績入力（メンバー別の稼働時間入力
> ＝3例目）を見てから再判断する。**

### ✅ チェックポイント2（マスタ）

- [x] メンバー・プロジェクトの追加→編集→削除制御が端から端まで動く ※未確認
- [x] ここで人間レビュー

> **★ チェックポイント2は「実装完了」であって「動作確認済み」ではない。**
> Task 5・6・7 の受け入れ基準8件すべてがブラウザ未確認のまま積み上がっている
> （ユーザーの判断により各タスクの検証はビルド確認までとしたため）。
> 人間レビューの前に、少なくとも以下は実機で通しておくこと:
>
> 1. **Task 3 基準2（未登録アカウントの拒否）** — 最優先。ここが動かないと
>    任意の Google アカウントで業績データにアクセスできる
> 2. Task 5・6・7 の追加→編集→削除→キャンセルの一巡
> 3. 削除ガード2種（メンバー: `t_costs` / プロジェクト: `t_sales`）。
>    どちらも実績データを SQL で手挿入する必要がある

---

## フェーズ4: 実績入力（コア機能）

### Task 8: 実績入力 — 条件選択と既存データ読込

**内容:** `/performance/input` のヘッダー（年度・月・プロジェクトのプルダウン、クエリパラメータ `?project=&year=&month=` 対応で一覧画面から遷移可能に）。選択が揃ったら該当の `t_sales`/`t_costs`/`t_status` を読み込み、売上行・費用行（全メンバー分の行＋管理費行）・備考の初期状態を構築する。既存データがない月は空フォーム（売上は「保守」「保守外（追加開発）」の2行）を出す。パンくず・最終更新表示。

**受け入れ基準:**

- [x] 年度・月・プロジェクトを切り替えると該当データが読み込まれてフォームに反映される ※未確認
- [x] データ未登録の月は初期2行＋全メンバーの空行が表示される ※未確認
- [x] クエリパラメータ付きURLで直接開ける ※未確認

**検証:** シードデータの入力済み月と未入力月を切り替えて手動確認。
**依存:** T4, T6, T7
**触るファイル:** `pages/performance/input.vue`, `composables/usePerformance.ts`, `lib/schemas/performance.ts`, `tests/unit/schemas/performance.spec.ts`
**規模:** M

> **★ Task 9 と統合して実装した**（ユーザーの判断）。テーブルを「表示専用 →
> 入力欄」と2度書き直す無駄を避けるため。以下の注記は Task 8・9 の両方を含む。
>
> **検証結果:** テスト155件が全件パス（T7までの136件 + 本タスクの19件）。
> `lib/` 全9ファイルのカバレッジは **Stmts・Branch・Funcs・Lines すべて100%** を維持。
> `make build`（`typeCheck: true`）も成功。
>
> **★ デザイン画像の数値例を全件検算し、一致を確認した**（Task 9 の受け入れ基準1）。
> 120.0h×¥60,000→15.0人日/¥900,000、40.0h×¥55,000→5.0/¥275,000、
> 0.0h→0.0/¥0、2.0h×¥60,000→0.25/¥15,000、12.0h×¥60,000→1.5/¥90,000、
> 80.0h×¥65,000→10.0/¥650,000。管理費¥120,000 を足した費用合計 ¥2,050,000、
> 売上 ¥3,000,000、粗利 ¥950,000（31.7%）まで画像と完全一致する。
>
> **★ 受け入れ基準はブラウザでの目視確認が未実施。加えて一部は現状のデータでは
> 検証できない。** task.md の検証手順は「シードデータの入力済み月と未入力月を
> 切り替えて手動確認」だが、**シードに実績データが存在しないため入力済み月が作れない**。
>
> - **いま確認できる**: 基準2（未登録月の初期2行＋全メンバー行）、基準3（クエリパラメータ）、
>   Task 9 の全基準（稼働時間を打てばその場で人日・金額・サマリーが動く）
> - **Task 10 の完成後になる**: 基準1のうち「実績のある月を読み込む」経路。
>   それ以前に確認するなら `t_sales`/`t_costs` に手動 INSERT が必要
>
> **★ USelect の落とし穴（知らずに書くと必ず踏む）:**
>
> `@nuxt/ui` の `USelect` はネイティブ `<select>` のラッパで、
> `update:modelValue` に `event.target.value`（＝**常に string**）を emit する。
> 一方 DB の `fiscal_year`/`month`/`project_id` はすべて `INT`。さらに
> `modelValue` と options の突き合わせは **`===` の厳密比較**で、外れても
> **例外を出さず黙って空表示に落ちる**。
> クエリパラメータ由来の `route.query.project`（`string`）を `id`（`number`）の
> options に当てると必ず外れるため、受け入れ基準3に直撃する。
> **選択状態は `ref<string>` で持ち、DB へ渡す直前にだけ `Number()` する**
> 変換層を1つ設けた。未選択は `null` ではなく **`''`**（`modelValue` の既定値が
> `""` で、`placeholder` は空文字の disabled オプションとして先頭に差し込まれる）。
>
> **★ クエリパラメータの復元は `fetchProjects()` の完了後に行う。**
> options が空の間に `modelValue` をセットしても USelect は表示できない。
> 存在しない ID（`?project=99`）も黙って空になるだけなので、実在チェックを自前で書いた。
>
> **★ 管理費行の判定は `user_id === null` を正とする。**
> `t_costs.cost_type` は `VARCHAR(50)` で **CHECK 制約がなく**、
> `'LABOR'`/`'MANAGEMENT'` は DDL のコメントによる申し合わせにすぎない。
> DB 側で構造的な根拠を持つのはテーブルコメント「user_id が NULL の行は管理費」
> だけなので、読み取りの判定はこちらに寄せた（保存時には `cost_type` も書き込む）。
> なお `user_id` が NULL の行は DB 上いくらでも作れるため、**複数あれば合算する**
> （先頭だけ採ると2行目以降の金額が画面から消えたまま洗い替えで失われる）。
>
> **★ `work_hours`/`work_days`/`unit_price` は NOT NULL ではない**（`DEFAULT 0` のみ）。
> 型も `number | null` なので、読込時に `?? 0` を必ず通す。
>
> **⚠ 稼働時間を小数第1位までに制限しているのは `NUMERIC(6,1)` に従っただけ（未確認）。**
> `t_costs.work_hours` が `NUMERIC(6,1)` なので第2位以下は DB 側で黙って丸められる。
> 通すと画面の表示値・計算した金額と保存値がズレるため画面で弾いているが、
> **この粒度が業務上妥当かはユーザー確認を取っていない**。SPEC 4.3 に入力粒度の
> 記述はなく、デザイン画像の例（2.0h / 12.0h / 120.0h）が第1位までだったため
> DDL をそのまま採用した。
> **ユーザーから「0.25h（15分単位）を入力したい」との指摘あり。**
> 対応には `NUMERIC(6,2)` へのマイグレーションが必要で、他の仕様変更とまとめて
> 別途行う方針（本タスクのスコープ外）。実績データはまだ0件なので、
> 変更するなら移行の手間はない。なお `work_days` は既に `NUMERIC(6,2)`。
>
> **確定した仕様判断:**
>
> | 論点 | 決定 |
> | --- | --- |
> | 年度プルダウンの範囲 | **今年度-2 〜 今年度+1 の4年度**（plan.md の残論点を解消） |
> | 初期選択 | **年度・月は今日から自動で埋める**。プロジェクトのみ未選択（レビュー指摘で変更） |
> | 単価の優先順位 | **実績値 > マスタ値**。`t_costs.unit_price` に保存する設計の意味 |
> | 検証のタイミング | 入力欄の **blur**。@input だと打ち終わる前に赤くなる |
>
> **★ 初期選択は年度・月を今日から埋める（レビュー指摘で変更した）。**
> 当初は「選んだことを明確にする」ため3つとも未選択にしたが、実績入力は
> 当月ぶんを入れる使い方がほとんどで、毎回2つ選ばせるのは操作が増えるだけだった。
> 年度は暦年ではなく `getCurrentFiscalYear()`（7月始まり）から採る。
> **プロジェクトだけは未選択のまま**——既定値を決めようがなく、先頭を勝手に
> 選ぶと意図しないプロジェクトの入力画面を開いてしまうため。
> クエリパラメータに `year`/`month` があればそちらが優先される
> （復元は「妥当な値があるときだけ上書き」なので初期値と自然に協調する）。
> 年度の境界をまたぐ日付（6/30・7/1・年またぎの1月）でも初期値が必ず
> 選択肢に含まれることを実測で確認済み。
>
> **設計上の判断:**
>
> - **`SalesBlock.vue` / `CostBlock.vue` の2部品分割はしなかった**（当初の「触るファイル」から外した）。
>   デザイン画像は売上・費用・粗利が**7列を共有する1つの表**（大項目/小項目/稼働時間/
>   稼働人日/単価/金額/小計）で、ブロックごとに `<table>` を分けると列幅が揃わず、
>   稼働時間・人日・単価の位置が売上ブロックとずれる。`<tbody>` を3つに分けて
>   グループ見出し行を置く形にした
> - **単価はマスタ値ではなく実績値を優先する。** `t_costs.unit_price` に単価を
>   保存する設計になっているのは、マスタ単価を後から変えても過去月の金額が
>   動かないようにするため。読込時にマスタを優先するとその意図が壊れる
> - **月の並びは `FISCAL_MONTHS`**（7月始まり）。数値昇順にすると 1,2,3… となり
>   年度の並びにならない（`lib/fiscalYear.ts` のコメントが明示）
> - **3テーブルは `Promise.all` で並列取得。** 直列に await すると往復が3回積み上がり、
>   プルダウンを切り替えるたびの待ちがそのまま3倍になる。索引
>   `idx_t_sales_fy_month_project` / `idx_t_costs_fy_month_project` は Task 2 が
>   「実績入力画面の読込（Task 8）」のコメント付きで用意済み
> - **`t_status` は `maybeSingle()`。** `UNIQUE (fiscal_year, month, project_id)` が
>   あるので0行か1行にしかならない。`single()` だと未登録月でエラーになる
> - **URL への反映は `replace`。** プルダウンを触るたびに履歴が積み上がると
>   戻るボタンが使い物にならない（`confirm.vue`/`AppSidebar.vue` と同じ理由）
> - **`calcLaborCost` には丸めた人日を渡す。** 稼働時間から一気に計算すると
>   画面の人日と金額の辻褄が合わない（`lib/calc.ts` のコメントに 300円ズレる実例）
> - **デザインの `PRJ-2025-008` 形式のコードは採用しない**（`m_projects` にコード列がなく
>   SPEC 5.1 のDDLにも根拠がない。Task 5 で ID のゼロ埋めを退けたのと同じ判断）。
>   サイドバーの「第2システム部」も A1 で部門を削除済みのため採用しない
> - **保存ボタンは置いたが `disabled`**（Task 10 で実装）。押せるのに何も起きない
>   状態は作らない

### Task 9: 実績入力 — 入力ブロックとリアルタイム計算

**内容:** 売上ブロック（金額入力＋任意名の行追加）、費用ブロック（メンバー別稼働時間入力→稼働人日・金額を`useCalc`で自動計算、単価は `m_users.unit_price` 初期表示だが上書き可能〔`t_costs.unit_price` に保存するため〕、管理費の直接入力行）、備考欄。ヘッダーに売上・費用・粗利（粗利率%）をリアルタイム集計表示し、稼働時間未入力メンバー数の「未入力N名」アラートを表示。

**受け入れ基準:**

- [x] 稼働時間入力で人日・金額・ヘッダーサマリーが即時再計算される（例: 2.0h→0.25人日、12h→1.5人日、120h×¥60,000→¥900,000）
- [x] 売上行の追加、任意小項目名の入力ができる ※未確認
- [x] 未入力メンバー数のアラートが入力状態に追随する ※未確認

**検証:** デザイン画像の数値例を再現して一致を確認。`npx vitest run` パス継続。
**依存:** T8
**触るファイル:** `pages/performance/input.vue`（`components/performance/` は作らなかった。理由はTask 8 の注記）
**規模:** M

> **★ Task 8 と統合して実装した。** 判断・落とし穴・設計上の理由はすべて
> Task 8 の注記にまとめてある。
>
> **基準1は計算を実測で検算済み**（デザイン画像の6行すべてと、費用合計・売上・
> 粗利・粗利率まで一致）。ただし**画面上での即時再計算はブラウザ未確認**。
> 基準2・3も未確認。

### Task 10: 実績入力 — 保存処理

**内容:** 「保存する」で対象（年度×月×プロジェクト）の `t_sales`/`t_costs` をdelete→insertで洗い替え、`t_status`（備考・updated_by）をupsert（SPEC 4.3 の一括更新/挿入）。稼働時間0/未入力のメンバー行は保存対象外。保存成功/失敗のフィードバック表示。保存後の再読込で入力内容が復元されることを保証する。

**受け入れ基準:**

- [ ] 保存→リロードで入力値が完全に復元される
- [ ] 2回目の保存で行が重複しない（洗い替えが機能する）
- [ ] 未入力メンバーの行は `t_costs` に作られない

**検証:** 保存→リロード→再保存のサイクルを手動確認し、SupabaseのテーブルをSQLで目視確認。
**依存:** T9
**触るファイル:** `composables/usePerformance.ts`, `pages/performance/input.vue`
**規模:** S

### ✅ チェックポイント3（コア機能）

- [ ] マスタ登録→実績入力→保存→再表示のコアフローが端から端まで動く
- [ ] ここで人間レビュー

---

## フェーズ5: 集計・可視化（破棄：SPEC-MODIFY-1で対応）

> **破棄理由:** `docs/SPEC-MODIFY-1.md` の「営業成績の閲覧はダッシュボードに限定」「プロジェクト一覧はドリルダウン入口」という責務分離により、Task 11-14 をそのまま実装すると責務が破綻する。  
> 詳細: `tasks/SPEC-MODIFY-1-PLAN.md` のフェーズ5.5「仕様変更v1（SPEC-MODIFY-1）」で Task 17-21 として再設計する。

### Task 11: プロジェクト営業成績一覧画面（`/projects`） — 破棄

**内容:** 年度・月・ステータス（成長/順調/注意/警告、A3）のフィルタ。当月KPIサマリー4種（当月売上・当月費用・営業利益・利益率、前月比付き）。プロジェクト一覧テーブル（プロジェクト名・売上・費用・営業利益・利益率・前月比・ステータスバッジ）。ステータスは `useStatusJudge` でクライアント判定（バッジ色はSPEC 6.2: 緑 `#22C55E` / 黄 `#EAB308` / 赤 `#EF4444`）。行アクションから「プロジェクト編集」「実績入力」（対象プロジェクト・年月をクエリで引き継ぎ）へ遷移。赤字はマイナス表示を赤色に。

**受け入れ基準:**

- [x] 破棄（SPEC-MODIFY-1: 4階層の第1階層「プロジェクト選択画面」として Task 19.1 で再設計）
- [x] 破棄（SPEC-MODIFY-1: 営業成績の一覧表示は Task 19.3「月選択」+ Task 19.4「実績閲覧」とダッシュボードに分離）
- [x] 破棄（SPEC-MODIFY-1: 編集導線は Task 19.4「実績閲覧」画面の「この月の実績を編集」ボタンで再設計）

**検証:** シードデータで判定4パターン＋前月なしパターンを作り、表示を目視確認。
**依存:** T4, T10
**触るファイル:** `pages/projects/index.vue`, `composables/useProjectSummary.ts`, `components/StatusBadge.vue`, `components/KpiCard.vue`
**規模:** M

### Task 12: ダッシュボード — 年度集計とKPIカード — 破棄

**内容:** 年度選択プルダウン（デフォルト今年度）。選択年度の `t_sales`/`t_costs` を全プロジェクト分取得し、月別・プロジェクト別に集計するcomposableを実装（A1により部門別集計はなし）。KPIサマリーカード4種（売上高合計・費用合計・営業利益合計・利益率）に前年同期比（%/pt、A6。過去データなしは「-」）を表示。

**受け入れ基準:**

- [x] 破棄（SPEC-MODIFY-1: KPIサマリは Task 20「ダッシュボード（俯瞰）」に統合）
- [x] 破棄（SPEC-MODIFY-1: 前年同期比ロジックは Task 20 で再利用）
- [x] 破棄（SPEC-MODIFY-1: 実績入力画面との合計一致は Task 20 で再検証）

**検証:** シードデータの手計算値と照合。集計ロジックはVitestで単体テスト追加。
**依存:** T4, T10
**触るファイル:** `pages/dashboard.vue`（または `pages/index.vue` リダイレクト）, `composables/useDashboard.ts`, `tests/unit/dashboard.spec.ts`
**規模:** M

### Task 13: ダッシュボード — 月次推移グラフ — 破棄

**内容:** Chart.js（vue-chartjs）を導入し、月次推移グラフを実装：売上・費用の棒グラフ＋利益率の折れ線グラフの複合グラフ（7月〜翌6月の12ヶ月）。`<ClientOnly>` でラップ。A1によりドーナツグラフはないため、グラフ領域は全幅で表示。

**受け入れ基準:**

- [x] 破棄（SPEC-MODIFY-1: 複合グラフは Task 20「ダッシュボード（俯瞰）」に統合）
- [x] 破棄（SPEC-MODIFY-1: 年度切替は年度マスタ（Task 17）経由で実装）

**検証:** dev環境で目視確認。
**依存:** T12
**触るファイル:** `components/dashboard/MonthlyTrendChart.vue`, `package.json`
**規模:** S

### Task 14: ダッシュボード — プロジェクト別月次マトリクス表 — 破棄

**内容:** プロジェクトごとに売上・費用・粗利・粗利率の4行×12ヶ月（7月〜翌6月）のマトリクス。未入力月は「-」。PJ別総合列（年間合計、粗利率は年間売上/粗利から算出）。単位は円固定（A4）。粗利・粗利率行はデザイン準拠の強調表示。A2によりPM名表示はなし。

**受け入れ基準:**

- [x] 破棄（SPEC-MODIFY-1: マトリクスは Task 20「ダッシュボード（俯瞰）」に統合）
- [x] 破棄（SPEC-MODIFY-1: 集計関数は Task 19.4 と Task 20 で再利用）
- [x] 破棄（SPEC-MODIFY-1: 横スクロールは Task 20 で再検証）

**検証:** シードデータの手計算値と照合。`npx vitest run` パス。
**依存:** T12（T13と並行可）
**触るファイル:** `components/dashboard/ProjectMatrix.vue`, `composables/useDashboard.ts`
**規模:** M

### ✅ チェックポイント4（全機能）— 破棄

- [x] 破棄（SPEC-MODIFY-1: チェックポイント4は Task 17-21・既存 Task 10/15/16 完了後に再設定）
- [x] 破棄（SPEC-MODIFY-1: 人間レビューは Task 17-21 完了後）

---

## フェーズ5.5: 仕様変更v1（SPEC-MODIFY-1）

> 詳細: `tasks/SPEC-MODIFY-1-PLAN.md` を参照。  
> スコープ: ① 年度マスタ作成 ② 4階層ドリルダウン（プロジェクト→年度→月→実績閲覧）③ `work_hours` を `NUMERIC(6,2)` に変更。

### Task 17: `m_fiscal_years` テーブル追加（年度マスタ）

**内容:** `m_fiscal_years` テーブル（`id`, `year INT UNIQUE`, `label VARCHAR(50)`, `status VARCHAR(20) DEFAULT 'ACTIVE'`, `created_at`, `updated_at`）を新規マイグレーションで追加。`status` は `'ACTIVE'` / `'CLOSED'`。RLSは既存の `is_app_user()` を流用し、SELECT/INSERT/UPDATE/DELETE の4ポリシーを追加。シードは今年度±2年の5件（今年度を `ACTIVE`、その他を `CLOSED`）。composable `composables/useFiscalYears.ts` で全件取得 + リアクティブな `fiscalYears` を提供。`lib/schemas/fiscalYear.ts` で年度追加のバリデーション（数値・範囲・一意性）+ `tests/unit/schemas/fiscalYear.spec.ts` を新設。`pages/performance/input.vue` の `FISCAL_YEAR_RANGE` を `useFiscalYears()` の戻り値に置換。

**受け入れ基準:**

- [x] `m_fiscal_years` テーブルが Supabase に作成され、RLSで未認証が0件になる
- [x] `pages/performance/input.vue` の年度プルダウンがマスタ由来になる

**検証:** Supabase SQL Editor / REST で認証あり・なしのアクセス結果を確認。`npx vitest run` パス。`npm run build` 成功。
**依存:** なし
**触るファイル:** `supabase/migrations/<ts>_add_m_fiscal_years.sql`, `supabase/seed.sql`, `composables/useFiscalYears.ts`, `lib/schemas/fiscalYear.ts`, `tests/unit/schemas/fiscalYear.spec.ts`, `pages/performance/input.vue`, `types/database.types.ts`
**規模:** S

### Task 18: AppSidebar のリンク更新

**内容:** 「プロジェクト一覧」の活性判定を `/projects` だけでなく動的セグメント（`/projects/[id]`, `/projects/[id]/[year]`, `/projects/[id]/[year]/[month]`）にも追従させる。`startsWith('/projects')` 系の判定で実装。リンクの文言は維持。

**受け入れ基準:**

- [x] 4階層のすべての画面で「プロジェクト一覧」が活性表示される ※ロジックはテストで確定。実機はTask 19後
- [x] `/projects/edit` でも「プロジェクト一覧」は活性のまま（既存挙動と一致）

**検証:** dev環境で各パスを巡回し、サイドバーの活性状態を目視確認。
**依存:** なし（Task 19.1 と並行可）
**触るファイル:** `components/AppSidebar.vue`, `lib/navActive.ts`（新規）, `tests/unit/navActive.spec.ts`（新規）
**規模:** S

> **★ 要件は既存コードで既に満たされていた。** `AppSidebar.vue` の `isActive` は
> Task 1 の時点から前方一致（`route.path === to || route.path.startsWith(\`${to}/\`)`）で
> 書かれており、4階層のドリルダウンにも初めから追従していた。
> SPEC-MODIFY-1-PLAN.md の「`startsWith('/projects')` の判定で吸収可能」という記述は、
> 実はこの既存実装のことを指していた。本タスクの起票時にそれを見落としていた。
>
> そこで**タスクの目的を「動くようにする」から「壊れないようにする」へ読み替えた**
> （ユーザー確認済み）。判定ロジックを `lib/navActive.ts` の純粋関数へ切り出し、
> 4階層すべてのパスを回帰テストで固定した。機能の振る舞いは1ミリも変えていない。
>
> **検証結果:** テスト172件が全件パス（T17までの159件 + 本タスクの13件）。
> `lib/` 全11ファイルのカバレッジは **Stmts・Branch・Funcs・Lines すべて100%** を維持。
> `make build`（`typeCheck: true`）も成功。
>
> **★ テストが実際に退行を捕まえることを、実装を壊して確認した**（テスト自体の検証）:
>
> | 壊し方 | 落ちたテスト |
> | --- | --- |
> | `startsWith(\`${to}/\`)` → `startsWith(to)` に緩める | `/projectsXYZ` の1件 |
> | 前方一致を消して完全一致に戻す | 4階層3件 + `/projects/edit` + `/members/edit` の5件 |
>
> **★ `startsWith(to)` ではなく `startsWith(\`${to}/\`)` である必然性。**
> プランの文言どおり素の `startsWith('/projects')` で実装すると、
> `/projectsXYZ` のような**別ルートまで活性になる**。区切りの `/`を必ず含めることで
> セグメント境界を守っている。`navActive.spec.ts` の `/projectsXYZ` のケースが
> この境界の番人で、緩めた瞬間に落ちる。
>
> **設計上の判断:**
>
> - **`route.path` を関数の引数に外出しした。** これが切り出しの実利で、
>   `useRoute()` に触れない純粋関数になるため Nuxt ランタイムを起動せずに
>   テストできる。`@nuxt/test-utils` を入れない Task 4 の方針と整合する
> - **`lib/` に置いた**（`composables/` ではない）。Vue のリアクティビティに
>   依存しない純粋関数で、Task 4 で `lib/calc.ts` 等を `lib/` に置いたのと同じ理由。
>   auto-import 対象外なので `AppSidebar.vue` で明示的に import している
> - **前方一致はプロジェクトだけの特別扱いではない。** `/members/edit` で
>   「メンバー」が光るのも同じ規則。テストにも両方入れて、規則が全ナビ項目に
>   効くことを固定した
>
> **★ 受け入れ基準1の実機確認は現時点では不可能。**
> `pages/projects/[id]/...` が未実装（Task 19.1〜19.4）のため、
> 4階層のうち3つは dev 環境で開いても 404 になる。**Task 19.4 完了後にまとめて
> 目視確認すること。** ロジック自体は上記のテストで固定済みなので、そのときの
> 確認は「テストが正しいことの追認」に相当する。
> 基準2（`/projects/edit`）は既存ページがあるため今でも確認できる。
>
> **やらなかったこと:** `pages/projects/index.vue` のカードリスト化（Task 19.1 のスコープ）、
> ナビ項目の文言変更（本タスクが「リンクの文言は維持」と明記）。

### Task 19: 4階層のルーティングと各画面

#### Task 19.1: プロジェクト選択画面（`/projects`）

**内容:** `m_projects` 全件をカードリスト表示（デザイン画像: `design/project-select.png`）。件数を画面上部に表示（例: 「全10件」）。右上「プロジェクト編集」ボタンで `/projects/edit` へ。カードクリック → `/projects/[id]` へ遷移。Reactivity: `onMounted` で取得、`pending`/`error` の2軸で状態管理（既存 `lib/fetchStatus.ts` パターンを踏襲）。

**受け入れ基準:**

- [x] `m_projects` 全件がカードリストで表示され、件数も表示される
- [x] カードクリックで `/projects/[id]` に遷移する
- [x] 「プロジェクト編集」ボタンで `/projects/edit` に遷移する
- [x] 0件 / ローディング / エラー の3状態が破綻なく表示される

**検証:** dev環境で `/projects` を開き、PJ 0件 / 1件 / 複数件で目視確認。
**依存:** Task 17（年度マスタと並行可）
**触るファイル:** `pages/projects/index.vue`, `components/Breadcrumbs.vue`（新規）
**規模:** S

> **★ `composables/useProjectsList.ts` は作らなかった**（当初の「触るファイル」から外した）。
> 既存の `useProjects.ts` に `fetchProjects` / `status` / `errorMessage` が揃っており、
> 分割代入で取得系だけ受ければそのまま使える。`pages/performance/input.vue` が
> `{ projects, fetchProjects }` だけを受けている先例と同じ形。新設すると同じ
> `select` を2箇所に持つことになり、`composables/` はカバレッジ対象外
> （`vitest.config.ts` で明示）なので重複を検知する手段もない。
>
> **★ `components/Breadcrumbs.vue` を前倒しで作った**（当初は実績閲覧画面の担当）。
> デザイン画像3枚（`project-select` / `year-select` / `performance-view`）で
> 要素数が1〜4と可変で、後続3画面すべてで使う。
> **リンクにするかは `to` の有無ではなく「最後の要素か」で判定している。**
> `to` だけで見ると、呼び出し側が末尾に `to` を渡したとき現在地が自分自身への
> リンクになる。最後は必ず現在地なので、そこで分岐させる。
>
> **`pages/performance/input.vue` の既存パンくずは置換していない。**
> あちらはリンクなしの `<p>` 1行で構成が異なり、置換すると input.vue 側の
> 回帰確認が要る。3画面で使われ方が出揃ってから寄せるほうが安全。
>
> **設計上の判断:**
>
> - **行は `<table>` ではなく `<div>` + `NuxtLink`。** 行全体がリンクなので、
>   `<tr>` を `<a>` で包むことになり HTML として不正になる。メンバー一覧の
>   `<table>` 構造はここでは踏襲できない
> - **サブタイトルの件数は取得成功時だけ出す。** ローディング中に「全0件」と
>   出ると、0件と読み込み中が区別できない
> - **状態分岐4つ（IDLE+LOADING / ERROR / 0件 / 一覧）はメンバー一覧を踏襲。**
>   `IDLE` をローディング側に含めるのは、`onMounted` 前の1フレームで
>   「0件です」が見えるのを防ぐため
>
> **★ 白いボタンは `color="white"` + `variant="solid"`（レビュー指摘で2度直した）。**
> `@nuxt/ui` の `variant` と `color` の組み合わせは直感に反する:
>
> | 指定 | 実際の背景 |
> | --- | --- |
> | `color="gray" variant="solid"` | `bg-gray-50`（灰色。白ではない） |
> | `color="gray" variant="outline"` | **透明**（`disabled:bg-transparent`）でページ地色が透ける |
> | `color="white" variant="solid"` | `bg-white` + `ring-gray-300`（これが正解） |
>
> `white` は `variant` ではなく **`color`** として定義されている
> （`node_modules/@nuxt/ui/dist/runtime/ui.config/elements/button.js` の
> `color.white.solid`）。`outline` を「白背景＋枠線」と思い込むと必ず外す。
> 実機で確認しないと、灰色と白の差はスクリーンショットの縮尺次第で見落とす。

#### Task 19.2: 年度選択画面（`/projects/[id]`）

**内容:** デザイン画像: `design/year-select.png`。`m_fiscal_years` 全件 + `t_sales`/`t_costs` を `project_id` で集計し、年度ごとの「年間売上」「年間粗利」を算出。3カラムグリッド。`getCurrentFiscalYear()` と一致する年度に「今年度」バッジを自動付与。「＋ 年度を追加」ボタンは `YearAddModal.vue` を開く（モーダル内で年度数値入力 + 「保存」/「キャンセル」ボタン）。保存: バリデーション（数値・範囲 1900-2999・一意性）後 `m_fiscal_years` に INSERT。成功: モーダル閉じて一覧再フェッチ。失敗: エラー表示。

**受け入れ基準:**

- [x] 対象PJの年度カードが3カラムで表示される
- [x] 「今年度」バッジがシステム日付ベースで正しく付与される
- [x] 「＋ 年度を追加」でモーダルが開き、年度入力 → 保存で年度マスタに追加される
- [x] 既存年度と重複する数値は保存時にエラー表示される
- [x] 売上・粗利が負値の年度は赤字表示（表記は円固定に変更。下記注記）

**検証:** dev環境でモーダル操作と年度追加を手動確認。`npx vitest run` パス。
**依存:** Task 17, 19.1
**触るファイル:** `pages/projects/[id]/index.vue`, `components/YearAddModal.vue`, `composables/useProjectYears.ts`
**規模:** M

> **検証結果:** テスト172件が全件パス（`lib/` を触らないため増減なし）。
> `make build`（`typeCheck: true`）成功。**受け入れ基準5件はすべてブラウザで実機確認済み。**
> 年度追加（2023年度）・重複エラー（2026年度）・範囲外（1899）・空欄・
> 存在しないID（`/projects/999/years`）・カード遷移まで一巡した。
>
> **★ ステータスバッジ（進行中/確定）の要件を削除した。**
> `20260810130000_drop_label_status_from_m_fiscal_years.sql` が
> 「ユーザー指示により label/status を不要として削除する」として両列を DROP 済みで、
> 現在の `m_fiscal_years` は `id` / `year` / `created_at` / `updated_at` のみ。
> **表示の根拠になる列が存在しない。** 当初の要件と `SPEC-MODIFY-1-PLAN.md` の記述は
> この削除より前に書かれたもので、実装可能性が失われている。
> デザイン画像の「進行中」「確定」バッジも同じ理由で採用しない。
> なお「今年度」バッジは `getCurrentFiscalYear()` から導けるので残した。
>
> **★ 金額は `-¥X.XM` ではなく円固定にした**（ユーザー確認済み）。
> デザインは `¥98.5M` の百万円短縮表記だが、plan.md A4 の「円固定」に揃え、
> 既存の `formatYen` をそのまま使う。新規の書式関数は作らない。
> 負値を赤くする点は維持している（`formatYen` は `-¥3,500,000` と符号を先頭に返す）。
> **Task 19.3 の月カードも同じ判断で揃えること**（デザインは同じく `¥7.5M` 表記）。
>
> **★ `tests/unit/useProjectYears.spec.ts` は作らなかった**（「触るファイル」から外した）。
> `vitest.config.ts` の `coverage.include` は `lib/**/*.ts` のみで、
> `composables/` は「Supabase クライアント等に依存し単体テストの対象外」と明記されている。
> 本タスクに新規の計算ロジックはなく、集計は既存の `lib/calc.ts`
> （`sumAmount` / `calcGrossProfit`、カバレッジ100%）を呼ぶだけ。
>
> **★ `<input type="number">` の `v-model` は数値を返す**（実装中に踏んだ）。
> `.number` 修飾子を付けなくても Vue が数値化するため、`ref('')` に
> 文字列前提で `.trim()` を呼ぶと `TypeError` で保存処理ごと落ちる。
> 空欄のときだけ `''` が来るので、型は `string | number` で受けて
> 保存時に一度だけ `Number()` する。
>
> **設計上の判断:**
>
> - **年度ごとにクエリを投げない。** `project_id` だけで `t_sales`/`t_costs` を
>   全年度分まとめて取り、`fiscal_year` で畳む。年度 N 件で 2N 回の往復になるのを避ける。
>   年度一覧と合わせて3本を `Promise.all` で並列に投げる
> - **重複は事前確認せず `23505` を捕まえる。** 確認と INSERT の間に他の利用者が
>   同じ年度を登録する競合を、往復を増やさずに塞げる。`fiscalYearInsertSchema` は
>   範囲しか見ておらず、一意性は DB の UNIQUE 制約に委ねる設計（Task 17 の判断）
> - **存在しない `[id]` で「見つかりません」を出す。** 放置すると名前が空のまま
>   カードだけ並ぶ壊れた画面になる。`FetchStatus.SUCCESS` になるまでは
>   「見つからない」と断定しない（取得前の1フレームで誤表示するため）
> - **パンくずはプロジェクト名を引けないとき項目ごと省く。** `'...'` を
>   置くと、存在しない ID の画面でその表示が固定されたまま残る

#### Task 19.3: 月選択画面（`/projects/[id]/[year]`）

**内容:** デザイン画像: `design/month-select.png`。`FISCAL_MONTHS`（7月始まり12ヶ月）を4カラムで表示。各月の「売上」「粗利」を `t_sales`/`t_costs` の集計で算出。未入力月は「未入力」と表示する（クリックは可能。下記注記）。「当月」バッジはシステム日付ベースで自動付与。クリック → `/projects/[id]/[year]/[month]` へ。

**受け入れ基準:**

- [x] 12ヶ月分が4カラムで表示される（4×3グリッド）
- [x] 未入力月は「未入力」と表示される（クリック可。当初の「グレーアウトしてクリック不可」から変更）
- [x] 「当月」バッジがシステム日付で正しく付与される
- [x] 売上・粗利が負値の月は赤字表示（表記は円固定。Task 19.2 と同じ）

**検証:** dev環境で目視確認。空月（実績0件）と入力済み月の2パターンで確認。
**依存:** Task 19.2
**触るファイル:** `pages/projects/[id]/[year]/index.vue`, `composables/useProjectMonths.ts`
**規模:** M

> **検証結果:** テスト172件が全件パス（`lib/` を触らないため増減なし）。
> `make build`（`typeCheck: true`）成功。**受け入れ基準4件はすべてブラウザで実機確認済み。**
> ただし実績が0件のため、確認できたのは空月のパターンのみ。
> **入力済み月の表示（売上・粗利の数値と赤字）は実績データが入るまで未確認。**
>
> **★ URL から `years` の重複を除いた**（ユーザー指示）。
> 当初の `/projects/{id}/years/{year}/months` は `years` が2回出て冗長なため、
> **`/projects/{id}/{year}/months`** に変更した（月選択・実績閲覧の両方）。
> **年度選択画面は `/projects/{id}/years` のまま**。
> ルートは `pages/projects/[id]/[year]/` に移した。
> `[year]` が `years` にもマッチしうる形になるが、月選択は `months` まで
> 一致を要求するので `/projects/{id}/years` が誤って月選択に入ることはない。
> `/projects/edit` も、Nuxt が静的セグメントを動的セグメントより優先するため
> `[id]` に飲み込まれない。3つとも実機で共存を確認済み。
>
> **★ 未入力月を「クリック不可」から「クリック可」に変更した**（ユーザー確認済み）。
> 実績データが0件の現状で当初の基準どおり実装すると、**12ヶ月すべてがクリック不可になり
> 実績閲覧画面（Task 19.4）へ到達する手段がなくなる**。「未入力」の表示は残したので
> 入力済みかどうかは区別できる。
>
> **★ 「当月」バッジは暦月だけで判定する**（ユーザー判断）。
> 年度は見ないので、どの年度を開いても同じ暦月にバッジが付く。
> 当初は年度も一致させる実装にしていたが、「当該年度を含まなくてよい」との
> 指示により暦月のみに変更した。
> なお `lib/fiscalYear.ts` に現在の月を返す関数はないため、暦月は
> `new Date().getMonth() + 1` で取っている（`pages/performance/input.vue` と同じ）。
>
> **★ 入力済み判定は行の有無（`hasRecords`）で行う。** 金額が0でも行があれば
> 入力済みなので、`totalSales === 0` では判定できない。
>
> **★ `month` は暦月（1〜12）であって年度内の連番ではない。**
> `docs/SPEC.md` 6.0 が「年度2026の `month = 1` は暦2027年1月」と明記している。
> 表示の暦年は `toCalendarYear(fiscalYear, month)` で導出しており、
> 7月始まりのため 1〜6月は翌暦年になる（実機で 2026年度の1月が「2027年」と
> 表示されることを確認済み）。
>
> **設計上の判断:**
>
> - **月は DB に持たず `FISCAL_MONTHS` を月マスタとして使う。** 実績のない月も
>   0 の行として必ず12件返すので、画面側に穴埋めのロジックが要らない
> - **0件の枝を作らない。** 年度選択画面と違い、月は常に12枚出る
> - **年度が `m_fiscal_years` に存在するかは問わない。** 実績だけある年度も
>   表示できたほうが素直で、年度マスタへの依存を増やさない
> - **集計は既存の `sumAmount` / `calcGrossProfit` を呼ぶだけ**なので、
>   Task 19.2 と同じ理由で composable のテストは作っていない

#### Task 19.4: 実績閲覧画面（`/projects/[id]/[year]/[month]`）

**内容:** デザイン画像: `design/performance-view.png`。上部サマリーカード（売上・費用・粗利（粗利率%））+ 詳細テーブル（7列: 大項目 / 小項目 / 稼働時間 / 稼働人日 / 単価 / 金額 / 小計）。`t_sales`/`t_costs` を売上/費用セクションに分けて表示。月ナビゲーション（`< 前月` / 当月 / `翌月 >`）ボタン。データ存在で活性化。`t_status.updated_at` + `t_status.updated_by` を「最終更新: YYYY/MM/DD HH:MM ユーザー名」形式で表示。**閲覧専用**。編集導線は「この月の実績を編集」ボタン → `/performance/input?year=${year}&month=${month}&project=${id}` 遷移。

**受け入れ基準:**

- [x] サマリーカード3種（売上・費用・粗利）と粗利率%が正しく表示される
- [x] 詳細テーブルが売上/費用/粗利の3セクションで表示される
- [x] 月ナビゲーションの前月/翌月ボタンが前月/翌月のデータ存在で活性化する
- [x] 「最終更新」が `t_status.updated_at` と `updated_by` から表示される
- [x] 「この月の実績を編集」ボタンで `/performance/input?year=...&month=...&project=...` に遷移する
- [x] テーブルは読み取り専用（入力欄なし）

**検証:** dev環境で目視確認。空月・入力済み月の2パターンで確認。
**依存:** Task 19.3
**触るファイル:** `pages/projects/[id]/[year]/[month].vue`, `lib/fiscalYear.ts`, `tests/unit/fiscalYear.spec.ts`
**規模:** M

> **検証結果:** テスト179件が全件パス（T19.3 までの172件 + 本タスクの7件）。
> `lib/` のカバレッジは **Stmts・Branch・Funcs・Lines すべて100%** を維持。
> `make build`（`typeCheck: true`）成功。
>
> **★ 実データで全基準を確認済み。** 保存処理（Task 10）が未実装で画面から
> 実績を入れられないため、`supabase/seed/*.csv` を用意して管理画面から取り込んだ
> （2023年度1月・プロジェクトID 1）。`/projects/1/2023/1` で次を確認した。
>
> | 項目 | 表示 |
> | --- | --- |
> | 売上 | ¥3,000,000 |
> | 費用 | ¥2,025,000（人件費 ¥1,905,000 + 管理費 ¥120,000） |
> | 粗利 | ¥975,000（32.5%） |
> | 最終更新 | 2026/08/10 23:47 冨永 隼人 |
>
> 明細も丸め規則どおり（120.0h→15人日→¥900,000、2.0h→0.25人日→¥15,000）。
> 稼働0のメンバーがグレーで並び、備考行も出る。
>
> **★ `db push --include-seed` では実績データを流せなかった。**
> 未適用のマイグレーションがあるときだけ `seed.sql` を実行する仕組みで、
> スキーマが最新だとハッシュだけ更新して SQL は走らない。
> 実績データはスキーマ変更を伴わないため、**CSV を管理画面から取り込む方式にした**
> （`supabase/seed/README.md` に手順と再取り込み時の DELETE 文を記載）。
> `t_sales`/`t_costs` は UNIQUE 制約がなく、再取り込みすると重複する点に注意。
>
> **★ URL からドリルダウンの階層語を全廃した**（ユーザー指示）。
>
> | 画面 | URL |
> | --- | --- |
> | プロジェクト選択 | `/projects` |
> | 年度選択 | `/projects/{id}` |
> | 月選択 | `/projects/{id}/{year}` |
> | 実績閲覧 | `/projects/{id}/{year}/{month}` |
>
> **`/projects/edit` と `/projects/[id]` が同じ位置に並ぶ**が、Nuxt は静的
> セグメントを動的セグメントより優先するため編集画面のまま。4つのURLすべてを
> 実機で開いて共存を確認済み。サイドバーの活性判定は `/projects` の前方一致
> なので影響を受けない。
>
> **★ `usePerformanceView.ts` は作らなかった**（「触るファイル」から外した）。
> 既存 `usePerformance` の `fetchPerformance` が3テーブルの並列取得・
> 単価の優先順位（実績値 > マスタ値）・管理費の合算をすでに正しく処理しており、
> 閲覧でもロジックは同じ。デザインも稼働0のメンバーをグレーで表示しているので、
> 「全メンバー分の行を作る」構造も噛み合う。
>
> **★ ただし空月の初期2行だけは閲覧で出さない。** `fetchPerformance` は
> 実績0件のとき「保守」「保守外（追加開発）」の2行を作る（入力用の仕様）。
> そのまま出すと存在しない売上が `¥0` で2行見えるので、
> `useProjectMonths` の `hasRecords` で実績の有無を見て空状態に振り分けた。
>
> **★ `lib/fiscalYear.ts` に `shiftFiscalMonth` を足した。**
> 月ナビは年度をまたぐ（7月の前月＝前年度6月、6月の翌月＝翌年度7月）が、
> 既存の `previousFiscalMonth` は年度初月で `null` を返す。
> あちらは前月比較の判定用でその仕様が正しいので**統合せず別関数にした**。
> 暦月の加減算で書くと 12月→1月 の折り返しと年度境界が二重に絡むため、
> 年度内の序数に直してから動かしている。
> 実機で 2026年度7月の前月が「6月」と出ることを確認済み。
>
> **設計上の判断:**
>
> - **年度をまたぐときだけ隣接年度へ問い合わせる。** 同一年度内は
>   `useProjectMonths` の `summaries` から引ける。年度端の2ケースのみ
>   その1ヶ月を `head: true, count: 'exact'` で存在確認する
>   （`useProjects.ts` の `hasPerformanceRecords` と同じやり方）
> - **`hasRecords` は `t_sales`/`t_costs` しか見ない。** 備考だけの月は
>   「未入力」扱いになるが、月選択画面と同じ基準なので画面間で矛盾しない
> - **粗利率は `toFixed(1)`。** `lib/format.ts` に率の書式関数はなく、
>   実績入力画面が同じ書き方をしているので揃えた
> - **稼働0のメンバーも行として残す**（グレー表示）。誰が動いていないかが
>   分かるほうが閲覧の役に立つ。デザイン画像もそうなっている

### Task 20: ダッシュボード（俯瞰）

**内容:** 営業成績をグラフとマトリクスで俯瞰。年度選択は Task 17 の年度マスタから。KPIサマリー（売上高合計・費用合計・営業利益合計・利益率）+ 前年同期比（%/pt）。月次推移グラフ（売上・費用の棒グラフ + 利益率の折れ線、Task 13 相当）。プロジェクト別月次マトリクス（4行 × 12ヶ月 × 全プロジェクト、Task 14 相当）。実績データ0件対応（`buildYoYComparison` の `null` 処理を活用）。

**受け入れ基準:**

- [ ] 年度切替でKPI・グラフ・マトリクスが再集計される
- [ ] 前年同期比の%・pt表記と増減の色分けが正しい。前年度データがない場合「-」
- [ ] プロジェクト別月次マトリクスが4行×12ヶ月で全プロジェクト分表示され、未入力月は「-」
- [ ] 実績データ0件でもKPI・グラフが破綻なく描画される

**検証:** シードデータで目視確認。集計関数はVitestで単体テスト追加。
**依存:** Task 17, Task 19.4（同じ集計関数を再利用）
**触るファイル:** `pages/dashboard.vue`, `composables/useDashboard.ts`, `components/dashboard/MonthlyTrendChart.vue`, `components/dashboard/ProjectMatrix.vue`, `tests/unit/dashboard.spec.ts`
**規模:** M

### Task 21: `work_hours` を NUMERIC(6,2) に変更

**内容:** マイグレーション追加（`NUMERIC(6,1)` → `NUMERIC(6,2)`）。`lib/schemas/performance.ts` の `costRowSchema` を変更（上限を `9999.99` に下げ、小数第2位まで許容）。`pages/performance/input.vue` の `step="0.1"` → `step="0.01"`。テスト19件差し替え（`2.25` 許容 / `2.256` 拒否 / `0.01` 許容 / 上限 `9999.99` テスト / `10000` 拒否）。`lib/calc.ts` のコメントも `NUMERIC(6,2)` に合わせて更新。`docs/SPEC.md` 5.1 の DDL 記述も更新。

**受け入れ基準:**

- [x] `t_costs.work_hours` が `NUMERIC(6,2)` になる
- [x] 0.25h（15分単位）が画面で入力でき、DB に保存される
- [x] `step="0.01"` で第3位以下は UI から入力不可
- [x] 既存のスキーマバリデーションテストがすべてパスする

**検証:** マイグレーションを Supabase に適用し、`t_costs` に `0.25` / `2.25` を INSERT できることを確認。`npx vitest run` パス。`npm run build` 成功。
**依存:** なし（Task 17 と並行可）
**触るファイル:** `supabase/migrations/20260811120000_alter_t_costs_work_hours_scale.sql`, `lib/schemas/performance.ts`, `tests/unit/schemas/performance.spec.ts`, `pages/performance/input.vue`, `lib/calc.ts`, `docs/SPEC.md`, `supabase/seed/README.md`
**規模:** S

> **検証結果:** テスト179件が全件パス。`lib/` のカバレッジは100%を維持。
> `make build`（`typeCheck: true`）成功。マイグレーションはリモートDBに適用済み。
> **受け入れ基準4件すべて実機確認済み。**
>
> **★ DB の挙動を実測で確かめた**（REST で試験行を入れて削除）。
>
> | 送信値 | 保存値 |
> | --- | --- |
> | `0.25` | `0.25`（移行前は `0.3` に丸められていた） |
> | `2.256` | `2.26`（第3位は DB 側で丸められる） |
>
> 第3位を画面で弾く必要があるのは後者のため。通すと表示値と保存値がずれる。
>
> **★ 上限が 99999.9 → 9999.99 に下がる**（ユーザー確認済み）。
> 桁数は6のままで小数部が1桁増えるので、整数部が1桁減る。月次の稼働時間としては
> 十分で、変更後は `work_days`（`NUMERIC(6,2)`）と上限が揃う。
> 既存データの最大は 120.0h だったため移行で桁あふれは起きなかった。
>
> **★ `roundTo` の実装は変更していない。** 新しい入力域
> （0.00〜400.00h を 0.01 刻みで 40,001 件）で数学的に正しい四捨五入と
> **不一致0件**であることを実測で確認した。一方 `toFixed(2)` は同じ域で
> **2,400件**、`Math.round(v*100)/100` は **286件** が食い違う。
> `lib/calc.ts` のコメントはこの新しい実測値に差し替えた（旧: 0.0〜200.0h で169件）。
> **数値ごと更新しないと、根拠の記述が実態と合わなくなる。**
>
> **★ 人日の丸めは現行のまま**（ユーザー確認済み）。0.25h → 0.03人日
> （0.03125 を小数第2位で四捨五入）→ ¥1,800（@60,000）。
> 厳密には ¥1,875 だが、`work_days` が `NUMERIC(6,2)` である以上
> 画面の人日×単価＝表示金額で検算が合う現行方針（plan.md A5）を維持する。
>
> **★ `step` は `work_hours` の1箇所だけ変えた。** 売上金額・単価・管理費の
> `step="1"` は整数の金額列で今回の変更と無関係。
>
> **実機で確認した内容**（`/performance/input?year=2023&month=1&project=1`）:
> `0.25` 入力 → 人日 `0.03` / 金額 `¥1,800`、`2.256` → 「小数第2位までで入力してください」、
> `10000` → 「稼働時間が大きすぎます」、既存データ（120/40/2/12/80）も正しく読める。

### ✅ チェックポイント5（仕様変更v1）

- [ ] Task 17-21 すべて完了し、ビルド / テストが緑（**Task 20 が未着手**）
- [x] 4階層のドリルダウンが端から端まで動く（`/projects` → `{id}` → `{year}` → `{month}`）
- [x] 年度マスタと実績入力画面の年度プルダウンが連動する（Task 17 で確認済み）
- [x] 0.25h の稼働時間が DB に保存できる（Task 21。REST で実測確認）
- [ ] ここで人間レビュー

> **★ 3画面の数値表示は実データで確認済み。** `supabase/seed/*.csv`
> （2023年度1月・PJ 1）を取り込み、年度カード（19.2）・月カード（19.3）・
> 実績閲覧（19.4）のいずれにも ¥3,000,000 / ¥975,000 が正しく集計されることを
> 目視した。入力済みと未入力の出し分けも意図どおり。
>
> **保存処理（Task 10）は未実装のまま。** 画面から実績を登録・更新できないので、
> 別の月やプロジェクトで確認したいときは CSV を足すか Task 10 を先に進めること。

---

## フェーズ6: 仕上げ・リリース

### Task 15: 仕上げ（ローディング・エラー・空状態）

**内容:** 全画面のローディング表示、Supabaseエラー時のメッセージ表示、データ0件時の空状態、保存系操作の二重送信防止を横断的に整える。スコープはこの4点のみ（機能追加はしない）。

**受け入れ基準:**

- [ ] ネットワーク遅延時にローディングが出て、失敗時にエラーメッセージが出る
- [ ] データ0件の年度でダッシュボード・一覧が破綻しない

**検証:** DevToolsのネットワークスロットリング/オフラインで手動確認。
**依存:** T11〜T14
**触るファイル:** 各ページ・composable（横断的軽微修正）
**規模:** S

### Task 16: Vercelデプロイと環境設定ドキュメント

**内容:** GitHub連携でVercelへデプロイ（SPEC 7章）。環境変数（`SUPABASE_URL`/`SUPABASE_KEY`）設定、Google OAuthのリダイレクトURL（本番/localhost）をSupabase・Google Cloud双方に登録。`docs/SETUP.md` に初期管理者インサート手順・環境構築手順を完成させる。

**受け入れ基準:**

- [ ] 本番URLでGoogleログイン→全機能が動作する
- [ ] `docs/SETUP.md` の手順だけで第三者が環境構築できる粒度になっている

**検証:** 本番環境での手動スモークテスト（ログイン→入力→保存→ダッシュボード表示）。
**依存:** T15
**触るファイル:** `docs/SETUP.md`, `vercel.json`（必要なら）, `nuxt.config.ts`
**規模:** S

---

## 廃止：旧保留セクション

旧「保留：スキーマ変更を伴う仕様変更」は **Task 21（`work_hours` を NUMERIC(6,2) に変更）** として実装するため、本セクションは廃止。  
追加のスキーマ変更は `tasks/SPEC-MODIFY-1-PLAN.md` または `tasks/task.md` のフェーズ5.5 に追記する。
